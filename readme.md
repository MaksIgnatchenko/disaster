1. cp docker-compose.yml.[enviroment] docker-compose.yml
2. docker-compose up -d
3. docker exec -ti CONTAINER bash
4. composer install
5. composer install
6. chown -R www-data:www-data /var/www
7. php artisan key:generate
